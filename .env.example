# Credence demo .env
# Runs with NoAuth using docker compose; nothing required to change.
# Required ONLY if you switch to JwtAuthProvider (see bottom section).

# App config (inside container)
CREDENCE_CONFIG=/app/config/config.yaml

# Database/Redis (container network URLs)
CREDENCE_DATABASE_URL=postgresql+psycopg://credence:credence@postgres:5432/credence
CREDENCE_REDIS_URL=redis://redis:6379/0
CELERY_BROKER_URL=redis://redis:6379/0
CELERY_RESULT_BACKEND=redis://redis:6379/0

# Rate limiting default (optional)
CREDENCE_RATE_LIMIT_DEFAULT=60/minute

# Plugin wiring (optional; defaults shown)
CREDENCE_PLUGINS__TRUST_FORMULA=credence.plugins.trust.formulas.default:DefaultTrustFormula
CREDENCE_PLUGINS__EVIDENCE_VALIDATOR=credence.plugins.evidence_plugins.default:DefaultEvidenceValidator
CREDENCE_PLUGINS__VERIFICATION_PROVIDER=credence.plugins.verification_plugins.default:DefaultVerificationProvider
CREDENCE_PLUGINS__DECAY_POLICY=credence.plugins.decay_plugins.default:NoDecayPolicy
CREDENCE_PLUGINS__LEADERBOARD_STRATEGY=credence.plugins.leaderboard_plugins.default:DefaultLeaderboardStrategy
CREDENCE_PLUGINS__AUTH_PROVIDER=credence.auth_providers.no_auth:NoAuthProvider

# REQUIRED ONLY if using JwtAuthProvider
# CREDENCE_PLUGINS__AUTH_PROVIDER=credence.auth_providers.jwt_auth:JwtAuthProvider
# CREDENCE_JWKS_URL=https://example.com/.well-known/jwks.json
# CREDENCE_JWT_ISSUER=https://example.com/
# CREDENCE_JWT_AUDIENCE=api://default